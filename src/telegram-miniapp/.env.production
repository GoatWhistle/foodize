# Build-time config for the production Mini App build (Vite loads it on build).
# Relative path -> browser hits the same origin; the miniapp nginx proxies
# /api/v1 to the backend. Do NOT hardcode a host here.
VITE_API_URL=/api/v1
